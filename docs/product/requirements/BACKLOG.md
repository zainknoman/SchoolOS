# SchoolOS — Requirements Backlog (DECIDED engineering work — NOT IMPLEMENTED)

> **Status:** PLANNED (decided, not built) · **Verified:** 2026-09-20 against `main@15362b7` · **Sources:** [OWNER-DECISIONS](../OWNER-DECISIONS.md), [KNOWN-GAPS](../../security/KNOWN-GAPS.md), [KNOWN-ISSUES](../../release/KNOWN-ISSUES.md), [BUSINESS-RULES](../BUSINESS-RULES.md) · **Owner:** Product Owner (priorities) / Engineering Lead (delivery)
> Everything here is **not** a current feature. Each item carries a decision already made by the owner (2026-09-20); the current behaviour stays documented as current until the item ships. Order and phasing: [GAP-ANALYSIS-AND-IMPLEMENTATION-PLAN](../../release/GAP-ANALYSIS-AND-IMPLEMENTATION-PLAN.md).
> **Priority:** `PILOT-BLOCKER` (must be done before a pilot goes live) · `PILOT-CORE` (in pilot scope, Q33) · `POST-PILOT` · `UNPRIORITISED` (no owner decision). **Waves/Phases:** W0 Foundations (BL-62, BL-65, BL-66, BL-37 part 1, BL-18 scaffold) · A Security & platform · B Org & tenancy · C People & lifecycle · D Academics · E Attendance & leave · F Fees · G Parent app · H Quality & pilot exit.
> **Owner rulings of 2026-09-20 (RD-1…RD-15) are incorporated**; the only remaining unknowns are placeholders/TBD vendors and legal values ([OWNER-DECISIONS](../OWNER-DECISIONS.md#remaining-unresolved-decisions--tbds-only)). Configuration values (domains, e-mail addresses, providers, account ids) are **always read from environment/config, never hard-coded**.
> Done for any item means: code merged via PR, tests added (unit + e2e where the rule is isolation/authorisation/data-integrity), docs regenerated, the referenced KG/KI/BR rows moved or updated.

## Wave 0 — Foundations (no product behaviour change; prerequisites for every later wave)
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-65 | *Done 2026-09-24 (`8cd67ab`):* `backend/migration-harness/` — scratch-database runner, legacy two-school fixture, reconciliation + idempotency checks, review CSV; one scenario per migration since (M1–M8, M12). **Migration test harness.** Builds a production-like **legacy dataset at the previous schema** (≥ 2 schools; global sessions/subjects shared across schools; parents with several children and duplicate-candidate CNIC/identifiers; `LEFT` students with and without a matching `TRANSFERRED_OUT` promotion; circulars/holidays without anchors; attendance rows), applies the target migration and backfill, and emits **reconciliation counts** (before/after per table) plus an **ambiguity / manual-review report** | PILOT-BLOCKER (gate for every data-migrating item: BL-01/02/03/20/23/60/61/25) | Runs against a **scratch** PostgreSQL only (refuses non-scratch URLs); a second run reports "no changes" (idempotent); expand and contract steps are tested separately; reports are machine-readable (JSON/CSV); runs in CI; rollback rehearsal = restore of the pre-migration dump verified | F9, RD-11, BL-62 |
| BL-66 | *Done 2026-09-24 (`695f4a1`):* `node scripts/docs/generate.mjs`, run after every item since. **Documentation generators, committed and reproducible (IMPLEMENTED 2026-09-20 on branch `wave-0/foundations`: `scripts/docs/`, `docs:generate`/`docs:check`/`docs:test`, CI drift check).** Scripts under `scripts/docs/` (invoked by a repo script) that produce ENDPOINTS, DATA-DICTIONARY, ERD, the generated parts of DATA-MODEL, MIGRATIONS (list/counts) and TEST-MATRIX (suite names/counts) from the repository; the earlier generators were throwaway local scripts and are not in the repo | PILOT-CORE (prerequisite for regenerating docs each wave) | One command regenerates all outputs from a clean checkout with no local paths; outputs are stamped with date and commit; a `--check` mode fails CI when generated docs drift; method-level `@Roles` overrides are handled (regression fixture for the earlier class-level scanner bug); reproduces the current docs' counts (185 handlers, 57 models, 14 enums, 13 migrations) before any code change | F8, docs/README rules |
| BL-37 (part 1) | *Done 2026-09-24 (`f2d2d81`):* backend lint/format backlog cleared (0 errors; warnings remain); blocking enforcement is part 2 (Wave 7). Formatting-only PR clearing the backend lint/format backlog **before feature PRs** (blocking enforcement is Wave 7) | PILOT-CORE | Lint/format clean; no behaviour change | Q46, KI-21 |
| BL-18 (scaffold) | Write the **failing e2e tests first** for BL-20, BL-01, BL-23, BL-60, BL-29, BL-64. *Done 2026-09-24:* `backend/test/pending/bl18-failing-first.e2e-spec.ts` (7 tests, `it.failing`; strict mode `E2E_PENDING_STRICT=1`) | PILOT-CORE | Tests exist and fail for the documented reasons | KI-22 |

## Phase A — Security and platform baseline
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-12 | Security headers (`helmet`), `trust proxy` config, dependency scanning in CI, fix High `npm audit` findings. *Done 2026-09-24 (branch `wave-0/foundations`):* `config/app-security.ts` (helmet + `TRUST_PROXY`), CI audit gate, 0 High (overrides for Prisma's `deepmerge-ts`/`mysql2`) | PILOT-BLOCKER | Response carries standard security headers; rate limiter keys on real client IP behind the proxy (e2e); CI fails on High advisories; `npm audit --omit=dev` shows 0 High | KG-5, KG-12, KG-13 |
| BL-51 | Config safety: reject `change-me` secrets; require explicit `NODE_ENV`; remove/disable dev fallbacks in production; never log reset links (KG-4). *Done 2026-09-24 (branch `wave-0/foundations`, `39e83a3`):* `config/env.validation.ts` wired into `ConfigModule`; unset `NODE_ENV` is strict everywhere; `LoggingMailAdapter` never logs bodies; `start:prod` path fixed | PILOT-BLOCKER | App refuses to boot in production with placeholder/short secrets or unset `NODE_ENV`; reset link never appears in logs in any mode; regression tests | KG-2, KG-3, KG-4 |
| BL-22 | Controlled first-SUPER_ADMIN bootstrap. *Done 2026-09-24:* `npm run bootstrap:super-admin` (`src/cli/bootstrap-super-admin.ts`), advisory-locked; seed refuses outside development/test | PILOT-BLOCKER | One-time command reads credentials from env/secret store; sets `mustChangePassword`; refuses to run once a SUPER_ADMIN exists (or is disabled by flag); audited; documented in DEPLOYMENT/RUNBOOKS; no public registration path | Q30, KI-23 |
| BL-21 | Server-side account disable/unlock, session revocation (revoke-all, logout), enforce `mustChangePassword` on the server. *Done 2026-09-24 (branch `wave-0/foundations`):* per-request recheck + `User.tokenVersion` (migration `20260924150000`), `PasswordChangeGuard`, `/auth/logout`, `/auth/logout-all`, `/admin/users/:id/{access,disable,enable,revoke-sessions}`; console revokes on sign-out and follows `PASSWORD_CHANGE_REQUIRED`. No console screen for disable/enable yet | PILOT-BLOCKER | Disabled/locked users are rejected within one request; `isLocked` is honoured; API rejects non-change-password calls while `mustChangePassword`; e2e | KG-10, KG-11, KG-23 |
| BL-10 | *Done 2026-09-25:* `S3StorageAdapter` (`STORAGE_DRIVER=s3`, required outside dev/test), `npm run storage:copy-to-s3` (size + SHA-256 verified, idempotent, missing-file report), tests against an s3rver emulator. S3-compatible object-storage adapter behind the existing storage interface **plus an idempotent, checksum-verified copy/migration tool** for existing files. **No schema redesign:** `File.storageKey` is already a unique randomised S3-style key, so existing `File` rows keep their keys; the tool copies each object from local uploads to the bucket under the same key, verifies size and checksum, is safely re-runnable, and outputs a missing-file report (rows whose file is absent — reported, never deleted). The local adapter remains for dev/test only | PILOT-BLOCKER | Uploads/downloads work against an S3-compatible endpoint in staging; access checks unchanged (`FilesAccessService`); production config performs no persistent local writes; the copy tool's second run is a no-op; e2e with a local S3 emulator; bucket/endpoint/credentials come from configuration (provider TBD) | Q40, Q28, KG-14, F6 |
| BL-39 | Safe scheduled jobs (leader lock/idempotency) and shared throttler storage. *Done 2026-09-25:* `JobLockService` (advisory lock) on both jobs, two-instance e2e; throttling documented as per-instance (single-instance pilot) | PILOT-CORE (needed before >1 instance) | Two instances never produce duplicate risk flags/digests (test with two app instances); throttling shared or documented single-instance | Q28, ADR-0008 |
| BL-11 | *Done 2026-09-25:* `observability/*` — health endpoints, request ids, JSON + access logs, global filter, Sentry-envelope reporter with scrubbing, alert rules in MONITORING-LOGGING. Health/readiness endpoints (DB check); structured JSON logs with request id; global exception filter; Sentry with PII scrubbing; uptime probe — monitoring must be **provider-agnostic** (uptime provider TBD, RD-4) and configured by environment | PILOT-BLOCKER | `/health/live` and `/health/ready` (DB) exist and are unauthenticated; logs are JSON with request id; Sentry events contain no passwords, tokens, CNIC, B-Form, medical data (scrub test); alert rules documented; no provider hard-coded | Q29, RD-4, KI-9, KI-24 |
| BL-13 | Deployment tooling **provider-agnostic**: container image, environment definitions (staging/production), migration step, TLS, secrets injection from a secrets manager; automated DB backups (PITR where available) with a **rehearsed provider-supported restore**. Minimum requirements per RD-3: managed PostgreSQL, automated backups, PITR where available, S3-compatible storage, TLS, secrets management, monitoring/logging, provider-supported restore | PILOT-BLOCKER | Staging and production deploy from a tag via a documented pipeline on the (TBD) chosen host without vendor-specific code paths in the application; backup meets RPO ≤ 24 h, retention ≥ 30 d; a restore rehearsal within RTO ≤ 4 h is recorded in `docs/release/` | Q25, Q26, RD-2, RD-3, KG-21 |
| BL-36 | Staff-console token-storage hardening review (HttpOnly Secure SameSite cookie, refresh rotation, CSRF) | PILOT-CORE (decision at security review; may be a later phase) | Written decision (adopt / accept-with-mitigation); if adopted: no regression in the current login flow, CSRF protection tested, tokens absent from `localStorage`; also remove query-string token acceptance (KG-15) | Q42, KG-9, KG-15 |
| BL-52 | Upload hardening: MIME sniffing, content-type allow-list, size limits, optional malware scan. *Done 2026-09-24:* `files/upload-inspection.ts` (magic-byte allow-list, extension must match, detected MIME stored), `files/malware-scanner.ts` (clamd INSTREAM, fail-closed), 10 MB limit unchanged | PILOT-CORE | Server-detected type must match allow-list; oversize rejected; tests | KG-14 |

## Phase B — Organization and tenancy foundation
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-20 | *Done 2026-09-25:* migration `20260925120000_m2_circular_holiday_school` + `MigrationReviewItem` (D8), `npm run backfill:m2` (rehearsed, scenario `m2-school-anchors`), scoped publish/fan-out/stats and holiday create/update/delete/visibility, console school pickers, e2e `school-anchors`. Fix cross-school leakage. **No reliable school anchor exists today:** `Circular` has only an optional `sectionId` (its `scope` is the string `school`/`section`) and `Holiday` only a nullable `campusId`; `circulars.service.ts:55-57` sends every school-wide circular to **all** `PARENT` users. *Schema (migration **M2**, expand → backfill → contract):* add `schoolId` to `Circular` and `Holiday` (nullable first, required in a later release). *Backfill:* Holiday from its campus's school; null-campus holidays and Circulars from `sectionId`→class→campus→school, else the author's `schoolId`; an author with no school (SUPER_ADMIN) or any ambiguous row goes to a **manual-review list — never guessed**. *Code:* school-wide recipients = parents linked through `StudentParent` to students enrolled in that school; holidays validate the caller's scope; a null-campus holiday means school-wide **within its own school** (**owner-confirmed 2026-09-20**). Regression tests for TENANT-1..5 | PILOT-BLOCKER | M2 applied additively with a reconciliation report; a parent/school never sees another school's circular/holiday (e2e cross-tenant); create with a foreign `campusId`/`schoolId` is rejected; ambiguous rows are listed for review; the contract step ships a release later | KG-1, KG-6, KI-22, F3 |
| BL-01 | *Done 2026-09-25:* migration `20260925150000_m3_school_sessions`, `npm run backfill:m3` (rehearsed, scenario `m3-school-sessions` + S5 refusal test), per-school sessions in the service/console, per-school active-session lookups, e2e `school-sessions`. **Gated by BL-62 (approved) and BL-65.** School-scoped academic sessions (executed only after the BL-62 strategy is approved); per-school "active" resolution; backfill existing data; remove global "first active" lookups; seed creates per-school sessions | PILOT-BLOCKER | `AcademicSession.schoolId` required; activating a session deactivates only that school's others; student create, voucher issue and bulk import resolve the caller's school session; migration backfills every existing row deterministically with a verification report; e2e for two schools | Q1, BR-ORG-01/02, KG-7, KI-1, BL-17 |
| BL-02 | *Done 2026-09-25:* migration `20260925180000_m4_school_subjects`, `npm run backfill:m4` (rehearsed, scenario `m4-school-subjects`), subjects API (list/create/rename/deactivate/delete-if-unused), subject guard on timetable/diary/assessment writes, console **Subjects** screen, e2e `subjects`. **Gated by BL-62 and BL-65 (migration M4).** Subject management: school-scoped `Subject`, unique per school, create/edit/deactivate by SUPER_ADMIN and own-school SCHOOL_ADMIN (teachers denied), reusable across campuses/classes, deactivate instead of delete when referenced | PILOT-BLOCKER | CRUD endpoints + console UI; `GET /subjects` scoped to school; e2e for authz and cross-school isolation; historical marks/timetable unaffected by deactivation | Q2, KG-8, KI-2 |
| BL-03 | *Done 2026-09-25:* migration `20260925210000_m5_school_fee_structures`, `npm run backfill:m5` (rehearsed, scenario `m5-school-fee-structures`), fee-structure lifecycle API (PATCH, no delete), issue checks + auto-lock + `FeeItem.feeStructureId`, term scoping via the session's school, console lifecycle actions, e2e `fee-structures`. Terms get no own `schoolId` column — their school is their session's (M3). **Gated by BL-62 and BL-65 (migration M5).** Terms and fee structures school-scoped; fee structure lifecycle draft → active → locked (once referenced) → archived; edit only in draft/unused; never hard-deleted | PILOT-BLOCKER | `schoolId` on both; lists scoped; edit of a used structure is rejected with a clear error; archive hides from new issue but keeps history; e2e | Q3, KG-8, KI-3 |
| BL-33 | *Done 2026-09-25:* copy extends to terms (date-shifted), assessment categories and timetable templates (teacher/subject assignments), same-school only, source untouched and idempotent (e2e `copy-structure`); console Sessions screen open to SCHOOL_ADMIN with a copy action. Subjects need no copying (school-level since BL-02); syllabus structure follows with BL-26. Copy-structure: console permission matches API (SCHOOL_ADMIN for own school; SUPER_ADMIN across schools); extend to subjects, subject assignments, syllabus structure, timetable templates; new-session records only | PILOT-CORE | SCHOOL_ADMIN can run it from the UI; copying never updates/deletes rows of the source or any historical session (test asserts source rows unchanged); idempotent; audited | Q19, KI-18, BR-ORG-05 |
| BL-32 | *Done 2026-09-26:* migration `20260926090000_bl32_staff_grants` (`StaffGrant` enum, `User.grants`, empty default), `@RequiresGrant` checked in `RolesGuard` (ACCOUNTS only), `GET /admin/users/accounts-staff` + `PUT /admin/users/:id/grants` (scoped, audited `account.grants`), parent→Accounts messages only to a granted user, grants in the session response; console route/nav gating + **Accounts Access** screen; ENDPOINTS marks grant-gated routes (e2e `staff-grants`). Permission model for ACCOUNTS (fees/finance only by default; other modules by explicit grant) | PILOT-CORE | ACCOUNTS default cannot reach admissions/complaints/messages APIs or routes; a grant mechanism exists and is audited; role matrix regenerated | Q18 |
| BL-53 | *Done 2026-09-26:* migration `20260926120000_m12_db_invariants` — data check that refuses on violations, partial unique `Enrollment_one_active_per_student`, unique voucher per student/session/month, `CHECK` constraints on six status columns (rehearsed, scenario `m12-db-invariants`; `M12_*` rows in `migration:dry-run`); voucher runs are one transaction and races give 409 (`rethrowUniqueAsConflict`), promotions likewise; e2e `db-invariants`. Status columns stay strings with DB checks rather than Prisma enums (no writer changes). Missing DB invariants: one ACTIVE enrolment per student; one voucher per student/session/month; status enums | PILOT-CORE | Unique/partial-unique constraints migrated after data check; concurrent-create test | KI-12, KI-13 |
| BL-62 | *Done 2026-09-24 (`bde3e83`, approval `5077e73`):* strategy approved (D1–D8), read-only `npm run migration:dry-run`, `MigrationReviewItem` queue. **Existing-school migration strategy and reconciliation report (RD-11)** — *Status 2026-09-24: strategy drafted in [MIGRATION-STRATEGY](../../database/MIGRATION-STRATEGY.md), dry-run extended to every F7 dependent and fee structures, `npm run migration:dry-run` added; **approved by the owner 2026-09-24 (D1–D8 as recommended)**.* — written and approved **before** BL-01, BL-02, BL-03, BL-20 (M2), BL-23 and BL-61 execute; its dry-run runs on the **BL-65 harness**. Explicit, auditable, backward-aware, idempotent where practical. Covers sessions, subjects, fee structures, circulars/holidays anchors, guardians and `LEFT` rows. Sessions: map each session to its school; a global session used by classes of several schools is **split per school** with dependents re-pointed (`Class`, `Enrollment`, `FeeVoucher`, `ReportCard`, `Term`, `Application`; keep a `legacySessionId`); never assign records to an arbitrary "current" session when ambiguous — manual review. Subjects: referenced subjects cloned per school and `Timetable`/`Assessment`/`DiaryEntry` re-pointed; the rule for unreferenced subjects is decided here. Guardians: deterministic matching on CNIC/identifier only; never merge on name similarity; flag candidates; never merge so as to expose one school's students to another school's staff | PILOT-BLOCKER (gate for BL-01/02/03/20/23/61) | Strategy doc approved; dry-run report lists mapped, ambiguous and duplicate-candidate rows with counts; manual-review queue defined; post-migration reconciliation totals match pre-migration counts; cross-school isolation e2e passes on the migrated copy | Q1, Q4, RD-11 |

## Phase C — People, guardians and lifecycle
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-23 | *Done 2026-09-26 (with BL-04, M6):* scoped parent detail/list (own school's children and counts only), PII boundary (profile/password/delete only when all the guardian's enrolled children are in the admin's scope, else super admin), link/unlink/lookup endpoints (`POST /admin/parents/lookup` exact identifier/CNIC, `POST/DELETE /admin/parents/:id/children`), messages resolved inside the child's school, `/me/children` + parent app labelled by school, super-admin dedupe report (`GET /admin/parents/duplicates`, never merges); circular/diary fan-out was already `StudentParent` ∩ school (e2e added); e2e `guardians`, graduated BL-18 test. **Guardian refactor** (gated by BL-62 and BL-65). *Corrected scope:* parent `User` rows already carry **no `schoolId`** (`create-parent-with-user.ts:33`); `User.identifier` and `ParentProfile.cnic` are already globally unique; access is only through `StudentParent` (BR-SCOPE-02). Therefore **no `schoolId` is added to the guardian identity, `ParentProfile` or a parent `User`**, and the identity/authentication account stay global. The actual work: (1) relationship data on `StudentParent` — `relationshipType`, `isPrimary`/`primarySlot` (schema M6, shared with BL-04); (2) **cross-school linking** — a second school linking an existing parent (found by identifier/CNIC) must learn nothing about that parent's children at other schools; (3) the **PII boundary** of a shared `ParentProfile` — which fields a school may read/edit versus school-scoped relationship/contact fields; (4) **school-scoped fan-out** for circulars, diary, messages, notifications (`StudentParent` ∩ school); (5) admin parent CRUD/visibility rules when a parent has children in several schools; (6) **deduplication** report on deterministic keys only (CNIC, identifier), never names; (7) parent-app child list labelled by school | PILOT-BLOCKER (owner ruling: not deferred) | One parent account sees children across schools; staff of school A cannot see school B's children or relationships of the same parent (cross-tenant e2e, written first); fan-out e2e per school; the dedupe report performs **zero auto-merges** and lists candidates; BL-65 harness test on legacy duplicate-parent data; no `schoolId` column added to the guardian identity | Q4, ADR-0006, BR-SCOPE-02, RD-11, F2 |
| BL-04 | *Done 2026-09-26:* migration `20260926150000_m6_guardian_links` + `npm run backfill:m6` (G4/G5, rehearsed), relationship type required on student create, admission approval, bulk import (`relationshipType` column) and new links; max two primary slots (409 on a third); console relationship/primary controls. *Schema M6 (shared with BL-23): `relationshipType` enum, `isPrimary`, `primarySlot` 1|2 with `@@unique([studentId, primarySlot])`; existing links get `OTHER` and are flagged.* Guardian rules: relationship type required (Father/Mother/Guardian/Other), primary flag, **max 2 primary per student**, unlimited non-primary/emergency contacts | PILOT-BLOCKER (with BL-23) | Third primary rejected (API + UI); relationship type mandatory; non-primary guardians allowed; bulk import respects rules; e2e | Q4, BR-STU-05 |
| BL-25 | *Done 2026-09-27 (`5225c4c`; follow-up `20260927170000_m8_history_fixes` in the BL-07 commit: removing a class teacher now closes the row, and deleting a class/school with open rows works):* migration `20260927150000_m8_teaching_assignment_history` (`TeachingAssignment`, deferred triggers on `Section.classTeacherId`/`Timetable`, immutability trigger) + `npm run backfill:m8` (rehearsed: scenario `m8-teaching-assignments`); `GET /teaching-assignments` (filters teacher/session/class/section/subject/role/current, school-scoped); console "Teaching History" tab on a teacher's staff profile; e2e `teaching-assignments`. Covers class teacher and subject teacher (timetable); non-teaching staff roles have no assignment source yet. Teacher/staff assignment history: teacher, session, school/campus, class, section, subject, role, start/end dates; queries for historical reporting; assignment changes end-date rather than overwrite | PILOT-CORE | New model + service; every change to class-teacher / subject / timetable assignment writes a history row; report "who taught X in session Y"; history rows immutable; e2e | Q15 |
| BL-05 | *Done 2026-09-27:* migration `20260927120000_bl05_promotion_policy` (`PromotionPolicy` per school, `StudentPromotion.conditions`/`indicators`, UPDATE-refusing trigger on `StudentPromotion`); preview returns results/attendance/fee indicators + warnings and no pre-selected decision; execute requires `confirmed: true`, conditions for `PROMOTED_WITH_CONDITIONS`, recomputes indicators and refuses a plain PROMOTED only when the school turned a rule into a block (409); `GET/PUT /promotions/policy` (audited `promotion-policy.update`); console indicators, bulk outcome, conditions field, rules panel, conditions in student history; e2e incl. rollover leaving prior-session data unchanged. Promotion: outcomes Promoted, **Promoted with conditions** (new value `PROMOTED_WITH_CONDITIONS`), Retained, Transferred, Withdrawn/Left, Graduated (kept, RD-10); computed indicators (results, attendance, fee clearance) shown as warnings; optional per-school blocking rules; explicit admin confirmation; history never modified. Enum terminology change is BL-61 | PILOT-CORE | Preview returns indicators per student and no auto-"Promoted" without confirmation; confirmation required in API; blocking only if the school configured it; conditions text stored; e2e including a session rollover that leaves prior-session data unchanged | Q5, RD-10, BR-ENR-02/03, KI-17 |
| BL-61 | *Done 2026-09-27:* migration `20260927090000_m7_lifecycle_statuses` (adds `StudentStatus.TRANSFERRED`, renames `TRANSFERRED_OUT`→`TRANSFERRED`, adds `PROMOTED_WITH_CONDITIONS`) + `npm run backfill:m7` (L1/L2, audited, rehearsed: scenario `m7-lifecycle-statuses`); a transfer promotion now writes `TRANSFERRED`; the profile API refuses `LEFT` (400); console uses the final terms and shows unmigrated rows as "Left (needs review)"; dry-run accepts both decision labels; e2e in `promotions`. **Lifecycle status migration (RD-10).** Final student statuses `ACTIVE, TRANSFERRED, WITHDRAWN, GRADUATED`; `PromotionDecision.TRANSFERRED_OUT` → `TRANSFERRED` (metadata-only enum rename); add `PROMOTED_WITH_CONDITIONS`. **Migration rule for existing data — `LEFT` is NOT renamed blindly:** a `Student` with status `LEFT` **and** a matching `StudentPromotion` with decision `TRANSFERRED_OUT` (latest promotion decides) → `TRANSFERRED`; every other `LEFT` row → **manual review** (exported list, no guess); new code stops writing `LEFT` (today `TRANSFERRED_OUT` writes it, `promotions.service.ts:55-57`); `LEFT` is removed from the enum only in a later contract migration once **zero** rows remain. **`EnrollmentStatus` is unchanged** — `COMPLETED` is the per-session closure of an enrolment, not a student lifecycle state. Previous status/session/class stay in history | PILOT-CORE (before BL-05 ships) | Migration uses the BL-65 harness: idempotent, transactional, reconciliation counts (matched → TRANSFERRED, unmatched → review) and a review list; no `LEFT` row changes without a matching promotion; API, both clients and generated docs use the final terms; `EnrollmentStatus` untouched (test asserts); rollback = restore; tested on a production-like copy | RD-10, KI-34 |
| BL-07 | *Done 2026-09-27 (M9 `20260927180000_m9_archive_retention`):* `archivedAt`/`archivedById`/`archiveReason` on Student and Staff, `archivedAt` on Teacher; `DELETE /admin/students|staff|teachers/:id` now **archive** (student: active enrolment → WITHDRAWN; teacher: off class-teacher/timetable slots, login disabled), archived records leave the lists (`?archived=true` lists them) and stay readable; `POST …/:id/unarchive`; `POST …/:id/erase` SUPER_ADMIN only (privacy-administrator role maps to SUPER_ADMIN until RD-6 names one), archived records only, audited, still refused while retained records reference them; sensitive columns listed once in `common/sensitive-fields.ts` (design note `docs/security/SENSITIVE-DATA.md`); console Archive/Show archived/Restore. Also closed KG-25: a `RecordScopeGuard` confines every `/admin/students/:id…`, `/admin/staff/:id…` and `/admin/teachers/:id` route to the caller's school/campus. Retention model: archived/former status for students and staff; soft delete only where legally required; hard delete of student/staff PII removed from the normal path; erasure restricted to SUPER_ADMIN or the **Privacy Administrator** role/permission (`[PRIVACY_ADMINISTRATOR]`), audited; **no automatic deletion**. Sensitive fields (CNIC and similar identifiers, medical/health) kept separable so encryption-at-rest and, if later required, field-level encryption can be added without redesigning the data model | PILOT-BLOCKER for removing hard delete | `DELETE /admin/students/:id` no longer hard-deletes (archives instead); archived records excluded from active lists yet visible historically; erasure endpoint behind role + audit; e2e; sensitive fields isolated behind an accessor/column boundary (design note reviewed) | Q7, RD-6, KG-17, KI-14 |
| BL-63 | *Done 2026-09-27 (M9):* `RetentionPolicy` with all 11 categories created unset; `GET/PUT /admin/retention-policy[/:category]` and `GET /admin/retention-policy/report` (SUPER_ADMIN, audited `retention-policy.update`; the report only counts); unit test asserts no scheduled job deletes records. Enforcement stays post-pilot. **Configurable data-retention policy (RD-6):** per-category retention settings for student, guardian, staff, attendance, academic results/report cards, fee/financial, complaints, audit logs, authentication/security logs, uploaded documents, backups. Periods are **unset (TBD, legal review)**; nothing is deleted automatically; the configuration only records approved periods and produces a review report | PILOT-CORE (configuration + report); enforcement POST-PILOT after legal approval | Settings persisted; every category present with period `unset`; no scheduled deletion exists in the codebase (test asserts); report lists records past a configured period without deleting | RD-6, Q7 |
| BL-41 | *Done 2026-09-26:* `GET /api/v1/admin/exports/:dataset` (students, guardians, enrolments, attendance, results, fees) returns CSV for **one school** (SCHOOL_ADMIN: their own school, a campus-level admin their campus; SUPER_ADMIN must pass `schoolId`), optional `campusId`/`academicSessionId`/`from`/`to`; attendance and fee rows are limited to the student's enrolments in scope, a guardian's links to other schools never appear; every export writes a `data-export.<dataset>` audit row (scope, filters, sensitive flag, row count, IP); `SENSITIVE_FIELDS` columns only with `includeSensitive=true` **and** the caller is the school's principal or SUPER_ADMIN (403 otherwise; the column lists fail closed); spreadsheet-formula cells neutralised; console **Operations → Data Export**; unit `data-export`, e2e `data-export`. Controlled data export for authorised school admins (students, guardians, enrolments, attendance, results, fees) with audit | PILOT-CORE | Export limited to caller's school; each export audited; sensitive fields (CNIC, medical) excluded unless explicitly requested and permitted | Q7, Q22 |

## Phase D — Academics
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-26 | Yearly syllabus per class + subject scoped to the academic session | PILOT-CORE | Syllabus CRUD by admin/authorised staff; unique per (class, subject, session); copied by BL-33; read by teachers of that class | Q2, Q19 |
| BL-27 | Configurable grading scales (per school): percentage bands → letter grade/remark; GPA fields in the model (optional); category weights must total 100 % to publish results | PILOT-CORE | Scale CRUD; final grade computation returns marks, %, letter; weights ≠ 100 blocks publication (fixes KI-16) | Q6, BR-GRD-01/03, KI-16 |
| BL-06 | Report cards generated from gradebook (subject-wise, overall, remarks) and exported as PDF; uploaded historical report cards still supported | PILOT-CORE | Generate per student/term/session from marks; regeneration after mark change is versioned (issued cards immutable); PDF export; parent read-only; e2e | Q6, BR-RC-01 |

## Phase E — Attendance and leave
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-28 | Attendance risk configurable per school (window, threshold, min days; defaults 30 / 25 % / 5), alerts to class teacher and school admin, optional parent notification (off by default) | PILOT-CORE | Settings persisted per school; job reads settings; alert visible to teacher and admin; parent notification only when enabled; unit + e2e; also clarifies "absence vs not-present" rate | Q8, BR-ATT-04 |
| BL-29 | *Partly done 2026-09-25 via BL-60: approval without a class teacher, LEAVE rows attributed to the approver. Remaining: recommendation step, M1b fields, audit split.* Leave workflow: teacher/class-teacher **recommendation**, SCHOOL_ADMIN approve/reject, no class teacher required. *Schema (migration **M1b**, additive):* `LeaveRequest` gains `recommendedById`, `recommendedAt`, `recommendationNote`, `decidedById` (User FKs), `decidedAt`, `decisionNote`; `status` stays a string until BL-53. *Audit semantics:* two separate `AuditLog` entries — `leave.recommend` and `leave.approve`/`leave.reject` — each naming the real acting user; a recommendation never changes `status`; the decision is final and recorded once. LEAVE attendance rows written on approval carry `markedByUserId` = the decider (via M1/BL-60) and never a class teacher's id unless the decider *is* that Teacher. Depends on BL-60 (M1) | PILOT-CORE | Approval succeeds for a section without a class teacher; recommender and decider are stored and audited separately; no synthetic teacher id; `leave.service.spec.ts:175` (asserts the old refusal) is **replaced**; e2e `leave` covers recommend→approve, approve without recommendation, reject, second decision → 400, no class teacher | Q12, BR-LV-02, KI-26, F5 |
| BL-60 | **Attendance marking without a class teacher (RD-8).** *Done 2026-09-25: migration `20260925090000_m1_attendance_marked_by_user`, services, `npm run backfill:m1` (rehearsed by harness scenario `m1-attendance-actor`), e2e `attendance-actor`.* *Root cause:* `Attendance.markedById` is a **required FK to `Teacher`** (`schema.prisma`, relation `AttendanceMarkedBy`). A TEACHER is attributed directly, but a SCHOOL_ADMIN/SUPER_ADMIN has no `Teacher` profile, so the service falls back to the section's class teacher (`attendance.service.ts` `markAttendance`/`markBulk` → `resolveClassTeacherIdFrom`) and **throws when the section has none**; leave approval writes LEAVE rows the same way (BL-29). The real actor exists today only in `AuditLog`. *Fix — schema first (migration **M1**):* make `markedById` nullable and add **`markedByUserId` (FK → `User`, the actual actor, set on every new write)**; set `markedById` **only when the actor is a Teacher**; **never synthesise or borrow a Teacher identity**. Backfill `markedByUserId` best-effort from `AuditLog` — the actions the code actually writes: `attendance.mark` (`entityId` = the row), `attendance.mark-bulk` (`metadata.date` + `studentIds`) and `leave-request.approve` (LEAVE rows in the request's range); there is no `attendance.update` action (corrected 2026-09-24, [MIGRATION-STRATEGY](../../database/MIGRATION-STRATEGY.md) rules A1–A3); unresolvable historical rows stay null and keep their `markedById`. A class teacher remains optional; when one exists the normal workflow is unchanged | PILOT-CORE (schema + code; M1 ships in Wave 1) | M1 is additive (both columns coexist; the previous build still works); an admin marks attendance in a section with no class teacher; new rows: admin → `markedByUserId` = admin, `markedById` null; teacher → both set; no fabricated teacher id anywhere; unit tests asserting the old behaviour are **replaced** (`attendance.service.spec.ts:113`); e2e regression in `timetable-attendance`: admin marks with no class teacher, teacher path unchanged, section with a class teacher unchanged, unauthorised caller rejected, holiday still blocks, audit names the real actor; every reader of `markedBy` (reports, dashboards, DTOs) tolerates null | RD-8, BR-ATT-03, KI-33, F1 |

## Phase F — Fees
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-08 | Pilot fee scope: structures/terms/charges, **manual payment recording and status**, vouchers and receipts, manual reconciliation, partial payments, outstanding balances, defaulter report, basic carry-forward to the next session/class; paid records immutable (corrections via reversing entries). Gateways stay **off** in the pilot (RD-14) and the fee model must let a gateway be enabled later without redesign | PILOT-CORE | A payment recorded manually creates a receipt and updates the voucher; partial payment allocates and leaves an outstanding balance; defaulter report per school/campus/class; carry-forward creates an opening balance in the new session without editing the old one; edit/delete of paid history rejected; pilot runs with all gateway flags off; e2e | Q9, RD-14, BR-FEE-01..03 |
| BL-24 | Post-pilot fee extras: discounts, scholarships, waivers, late fees, refunds, installment plans, accounting/export reports | POST-PILOT | Each is a separate, audited, reversible ledger entry; no mutation of paid history | Q9 |
| BL-14 | Integration verification: **FCM** in staging (pilot; needs `[FIREBASE_PROJECT_ID]`); **SMTP** when a provider is chosen (e-mail/SMS may stay disabled in the pilot, RD-4); JazzCash/EasyPaisa **off in the pilot** (RD-14) and verified post-pilot behind feature flags | PILOT-CORE (FCM) / POST-PILOT (SMTP if deferred, gateways) | Sandbox run recorded per integration in `docs/release/` with date and evidence; all provider flags default off; the core platform starts and works with every integration disabled | Q34–Q36, RD-4, RD-12, RD-14, KG-18 |

## Phase G — Parent app
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-35 | Parent password reset: separate flow from staff reset, deep-link/web-compatible URL built from configuration, short-lived single-use token | PILOT-BLOCKER | Reset link opens the parent app (or parent web page) and completes; token single-use; staff and parent flows separate in code; base URL/deep-link scheme from config (`[PRODUCTION_DOMAIN]`); e2e. **Requires an e-mail provider** — if none is chosen for the pilot use BL-64 | Q41, KI-7, RD-4 |
| BL-64 | *Depends on BL-21 (server-side `mustChangePassword`).* **Admin-assisted parent password reset (pilot fallback when no e-mail provider is configured):** a school admin issues a one-time temporary password with `mustChangePassword`; nothing is written to logs; audited; disabled by config when e-mail reset is active | PILOT-BLOCKER unless an SMTP provider is chosen | Admin can reset a parent's password without e-mail; parent must change it at first login (server-enforced, BL-21); no token/password in logs; rate-limited; e2e. *Done 2026-09-24:* `POST /admin/parents/:id/reset-password` (`ADMIN_PASSWORD_RESET`), console action, parent-app change-password screen | RD-4, KG-4, B-1 |
| BL-30 | Parent complaints (**pilot scope, RD-9**): parent submits a complaint with category, title, description and attachments (where file storage supports it); status; assigned owner; internal notes; resolution/comments; created/updated timestamps; audit trail. Parents see their own complaints, status and resolution comments only | PILOT-CORE | Parent-facing DTO excludes internal notes (test); a parent sees only complaints about their own children; staff can assign an owner, add internal notes, respond and change status; every change is audited; attachments use the storage service (BL-10) | Q10, RD-9, KI-27 |
| BL-31 | Complaints (**post-pilot, RD-9**): multi-level escalation, SLA automation and escalation timers, complex routing rules, automated notifications/workflows, advanced analytics, cross-campus complaint administration, complex approval chains | POST-PILOT | The pilot design (BL-30) must not block these: keep status/assignment as data, not hard-coded flows | Q10, RD-9 |
| BL-43 | Parent app release pipeline: Play Console (organisation-owned, `[PLAY_DEVELOPER_ACCOUNT]`), signing keys outside Git, Firebase staging/production projects (`[FIREBASE_PROJECT_ID]`, owner `[FIREBASE_OWNER]`), production app identifiers; ownership and access documented before publishing | PILOT-CORE | Signed AAB from CI/secure store, key never in repo; production build points to production API; internal-testing track release recorded; access list documented | Q32, Q35, RD-12 |
| BL-54 | Parent app device/accessibility validation: Android 9+, low-memory, slow network, small screens; Android accessibility checks | PILOT-CORE | Test matrix executed and recorded; no blocking defects | Q43, Q45 |

## Phase H — Quality, verification and pilot exit
| ID | Item | Priority | Acceptance criteria | Refs |
|---|---|---|---|---|
| BL-37 | CI hardening: **prerequisite** — clean the backend format/lint backlog (2,014 errors); then make backend lint, format, type-check, unit, e2e and build blocking | PILOT-CORE (no fixed date) | Backend lint job has no `continue-on-error` and is green; documented | Q46, KI-21 |
| BL-15 | Load/performance validation against Q44 targets (p95 < 500 ms CRUD, auth p95 < 1 s, 100 concurrent, 99.5 % availability plan) | PILOT-BLOCKER for exit | Load test report against staging with 2,000 students' data | Q44 |
| BL-40 | Pagination/filters on unbounded list endpoints. *Done 2026-09-25:* six people/admissions lists paged + searchable (headers, array body unchanged, 2,000-row unpaged cap), `EntityTable` server mode used by the student and parent tables | PILOT-CORE | List endpoints accept `page/limit` with server caps; consoles updated; e2e | KI-8 |
| BL-18 | E2E for staff, hiring, files, risk job; regression tests for every closed KG/KI | PILOT-CORE | Suites in CI | KI-22 |
| BL-55 | Accessibility conformance pass (WCAG 2.1 AA) for staff console | PILOT-CORE | axe checks in CI plus manual keyboard/screen-reader audit report | Q43 |
| BL-56 | Privacy operations: **privacy notice before production use**, consent/notice requirements per legal review, breach-handling incident process, incident/support process with internal targets P1 ≤ 30 min, P2 ≤ 2 business h, P3 ≤ 1 business day, P4 ≤ 3 business days (internal, not contractual — RD-13), support channel `[SUPPORT_EMAIL]`; owned by `[PRIVACY_ADMINISTRATOR]`; counsel review; **no invented legal text and no compliance claims**; breach-notification timelines TBD pending legal review | PILOT-BLOCKER (non-code) | Documents exist with placeholders, approved by the Product Owner; counsel review recorded before real data is entered; incident process rehearsed once | Q22, Q27, RD-7, RD-13 |
| BL-57 | Pilot exit review: security review, backup restore test, monitoring live, school sign-off, no open Critical/High defects | PILOT-BLOCKER for exit | Signed checklist in `docs/release/` | Q33 |
| BL-34 | *Done 2026-09-26:* seed → *Demo School North/South* (`DSN`/`DSS`), neutral campus names, `Class 1–8`, `@schoolos.local`; `.env.example` + CI DB name `schoolos`; Postman; test fixtures; Firebase placeholder project id; console locale key `schoolos.locale` (legacy key still read). **Keep/rename decisions:** existing local databases — *keep* (only `.env.example` changes; nothing to migrate); package names `backend`/`staff-console`/`parent_app` — *keep* (neutral); Android/iOS ids already `pk.edu.schoolos.*` — *keep*; real Firebase project — *later* (BL-43; note `firebase_options.dart` iosBundleId `…parent_app` ≠ Xcode `…parentApp`, regenerate with `flutterfire configure`); archived/historical docs and the harness refusal list — *keep*; design wireframes — labels changed to *Demo School*. **SchoolOS rebrand of config/code + neutral demo data.** Inventory: `backend/prisma/seed.ts` (emails `@schoolportal.local`, DB name, "Beacon House"), `backend/.env.example`, `.github/workflows/ci.yml`, `docs/api/*.postman_*.json`, `parent-app/lib/firebase_options.dart`, `backend/src/notifications/fcm-config.spec.ts`, `staff-console/src/lib/i18n.ts`, `staff-console/src/views/StaffManagementView.spec.ts`. **Risk-review separately:** default DB name (existing databases), Firebase/app identifiers (need a new Firebase project anyway, BL-43), package names, `docs/database/SEEDING.md` after seed change | PILOT-CORE | No product-facing "SchoolPortal"/"Beacon House" outside archives; seed uses `Demo School` and `@schoolos.*` demo domains; technical identifiers with migration risk are listed with an explicit keep/rename decision | Q17, Q23, KG-20, KI-20, BL-17 |
| BL-58 | Repository hygiene — **documented only; no cleanup is authorised (RD-15).** Recommended actions live in the [housekeeping inspection](../../audit/2026-09-20-housekeeping-inspection.md): stale worktrees/branches (keep `staff-hiring-console-ui` and `stash@{0}`), large design binaries (LFS/external), `sample4` CSV scratch content (contains another project's credentials — must not be committed). A separate controlled housekeeping phase needs explicit authorisation | PILOT-CORE (housekeeping) — waiting on authorisation | Executed only after written authorisation; no unique unpushed work lost | Q47–Q49, RD-15, KI-25 |

## Post-pilot / unprioritised
| ID | Item | Priority | Notes | Refs |
|---|---|---|---|---|
| BL-45 | Per-period attendance (design constraint now: do not block it; keep day-level uniqueness migration-friendly) | POST-PILOT | | Q11, KI-15 |
| BL-46 | Public online admissions API/module | POST-PILOT | | Q14 |
| BL-47 | Student login (architecture must allow it; do not implement) | POST-PILOT | | Q13 |
| BL-48 | WhatsApp Business Cloud API with approved templates | POST-PILOT | Replace the free-text sender | Q38 |
| BL-49 | AI drafting hardening: feature flag default off, cost limits, redaction | POST-PILOT | | Q39 |
| BL-38 | SMS provider adapter (decouple from the placeholder URL; provider configurable via environment; **TBD**) | POST-PILOT (SMS disabled for the pilot, RD-4) | Business logic depends only on an interface; provider chosen by configuration; fake sender in tests | Q37, KI-4, RD-4 |
| BL-59 | Campus-level attendance-risk overrides; advanced accounting; advanced analytics | POST-PILOT | | Q8, Q33 |
| BL-09 | Parent web portal | UNPRIORITISED | Q41 requires the reset flow to be app-or-web compatible only | — |
| BL-16 | Multi-tenant SaaS direction | UNPRIORITISED | Needs its own audit before any refactor; BL-23 and BL-01 must not foreclose it | — |
| BL-19 | OpenAPI/Swagger contract | UNPRIORITISED | | KI-11 |

## Legacy ID map (former backlog rows)
BL-01…BL-22 keep their meaning; **BL-65 and BL-66 were added 2026-09-20 (Wave 0 correction pass)** — BL-42, BL-44 and BL-50 were never assigned; BL-04 = guardian **rules** (identity refactor is BL-23), BL-08 = pilot fee scope (extras moved to BL-24), BL-14 split by phase, BL-17 folded into BL-01/BL-34, BL-13/BL-11/BL-10 re-scoped to the decided hosting-agnostic targets.
