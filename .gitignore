# --- agentic-suite (auto-managed) — keep plan/docs, ignore run machinery ---
plan/*
!plan/docs/
.agentic-builder/

# --- personal Claude Code settings overrides ---
.claude/settings.local.json

# --- local git worktrees (scratch, not part of the repo tree) ---
.worktrees/
.claude/worktrees/

# --- auto-generated tool hook config, not part of the repo ---
.github/hooks/

# --- subagent-driven-development scratch (ledger, briefs, review packages) ---
.superpowers/

# --- monorepo build output / deps / local env ---
**/node_modules/
**/dist/
**/coverage/
**/.env
!**/.env.example
backend/dev.db
backend/*.db-journal
backend/prisma/dev.db
backend/prisma/*.db-journal
backend/uploads/
**/*.log
